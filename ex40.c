/*
* If i and j are positive integers, does (-i)/j always have the same value as -(i/j)?
* justify your answer.
*/
main(){

    int i = 10, j = 5;
    printf("%d\n", (-i)/j);
    printf("%d\n", -(i/j));
}
// always negative number
